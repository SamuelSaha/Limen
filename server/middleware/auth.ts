/**
 * Server middleware: attach session to event context.
 * Placeholder — replace with Better Auth session resolution.
 */
export default defineEventHandler(async (event) => {
  // TODO: resolve session from cookie via Better Auth
  // event.context.session = await resolveSession(event);
});
